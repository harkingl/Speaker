<resources>
    <!-- Base application theme. -->
    <style name="Theme.Speaker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SpeakerAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ActivityDialogStyle">
        <!--隐藏action bar和title bar-->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!--背景透明-->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="Spinner">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/base_spinner</item>
        <item name="android:indeterminateDuration">1000</item>
        <item name="android:indeterminateOnly">true</item>
    </style>

    <declare-styleable name="TitleTagView">
        <!-- 名称 -->
        <attr name="name" format="string"/>
        <attr name="textSize" format="dimension"/>
    </declare-styleable>

    <declare-styleable name="ConstraintHeightView">
        <attr name="maxHeight" format="dimension"/>
    </declare-styleable>

    <style name="DialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowSoftInputMode">stateAlwaysVisible|adjustPan</item>
    </style>

    <declare-styleable name="WaveLineView">
        <!--backgroundColor
        color of the view's background -->

        <!--lineColor
        color of the wave lines -->

        <!--thickLineWidth
        the center thickLines' stroke width-->

        <!--fineLineWidth
        the three fineLines's stroke width-->

        <!--moveSpeed
        the wave line animation move speed from left to right
        you can use negative number to make the animation from right to left
        the default value is 290F,the smaller, the faster
        -->

        <!--samplingSize
        The number of sampling points, the higher the more fine,
        but higher than a certain limit the naked eye is difficult
        to distinguish, the higher the lower the efficiency of rendering
        -->

        <!--sensibility
       Sensitivity, the bigger the more sensitive [1,10]
       the default value is 5
       -->
        <attr name="wlvBackgroundColor" format="color"/>
        <attr name="wlvLineColor" format="color"/>
        <attr name="wlvThickLineWidth" format="dimension"/>
        <attr name="wlvFineLineWidth" format="dimension"/>
        <attr name="wlvMoveSpeed" format="float"/>
        <attr name="wlvSamplingSize" format="integer"/>
        <attr name="wlvSensibility" format="integer"/>
    </declare-styleable>

    <declare-styleable name="AudioAnimalView">
        <attr name="paint_color" format="color" />
        <attr name="animation_lenght">
            <enum name="min" value="0" />
            <enum name="mid" value="1" />
            <enum name="max" value="2" />
        </attr>
    </declare-styleable>

    <style name="SyncProgress">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/sync_progress</item>
        <item name="android:indeterminateDuration">1000</item>
        <item name="android:indeterminateOnly">true</item>
    </style>

    <style name="CommonDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>